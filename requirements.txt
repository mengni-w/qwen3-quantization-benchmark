# Core dependencies / 核心依赖
numpy>=1.21.0
pyyaml>=5.4.0
aiohttp>=3.8.0
tqdm>=4.62.0
requests>=2.28.0

# vLLM and related / vLLM 及相关
# Note: Install vLLM separately based on your CUDA version
# 注意：根据您的 CUDA 版本单独安装 vLLM
# vllm>=0.5.0

# Evaluation dependencies / 评估依赖
lm-eval>=0.4.0  # For additional datasets / 用于其他数据集
regex>=2022.1.18  # For text parsing / 用于文本解析

# Development dependencies (optional) / 开发依赖（可选）
pytest>=7.0.0
black>=22.0.0
flake8>=4.0.0

